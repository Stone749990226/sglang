# megaAttention 接入 SGLang(Qwen3 MoE)设计

- 日期:2026-06-30
- 状态:已确认核心接入策略,待进入实现计划
- 目标硬件:8×H200(SM90)+ NVSwitch/NVLS
- 首个端到端模型:Qwen3-30B-A3B(`/models/Qwen3-30B-A3B`),TP4
- 后续扩展:Qwen3-235B-A22B,TP8(per-rank head 形状与 30B-A3B@TP4 同为 `H8/kv1`)

## 1. 目标与范围

把 `megaAttention`(`3rdparty/megaAttention`)的 Hopper SM90 fused persistent kernel
`causal-varlen-FA → O_proj → tensor-parallel NVLS AllReduce` 接进 SGLang,在 Qwen3 MoE
模型的 **prefill 阶段**替换 `self.attn` + `self.o_proj` + 推迟的 TP AllReduce。允许破坏
SGLang 的封装性,目标是先把端到端跑通。

**已确认的关键决策**:

- **全融合替换**:prefill 路径直接调用 megaAttention 的 `launch_layer_prefill`,吃掉
  attention、o_proj、AllReduce 三段;输出已是 all-reduced 的 `[tokens, hidden]`。
- **混合 decode**:megaAttention 不支持 decode(roadmap 未做)。decode 走 SGLang 现有
  后端(`fa3`)+ 普通 `o_proj`。
- **首版只覆盖 PD 分离下的 prefill worker**:megaAttention 只进入普通 prefill/extend
  forward,不覆盖 decode worker,也不覆盖 mixed/spec/draft/dLLM/split-prefill 等非普通
  EXTEND 模式。
- **直接上全融合 AR**:megaAttention 自己用 NVLS 做 AllReduce(`tp_size=TP`),并跳过
  SGLang 推迟的那条 attention AllReduce。不走"先 tp_size=1 让 SGLang 做 AR"的过渡阶段。
- **megaAttention 子模块测试不作为本仓库接入前置**:用户已单独验证 megaAttention 可运行;
  SGLang 侧仍需做集成 smoke / 单层数值 / e2e 验证。

**不在本设计范围内**:decode 用 megaAttention;非 PD-prefill 普通 EXTEND forward;
`page_size != 128`;dp-attention / CP / EP / 量化通信路径;非 Hopper 架构;
megaAttention 自身 kernel 的修改(如发现接口不匹配,单独反馈给用户在 megaAttention 侧改)。

## 2. 背景:两边接口

### 2.1 megaAttention 对外接口

SGLang-facing 接口是 `mega_attention.runtime.FusedFaOprojArWorkspace`
(`3rdparty/megaAttention/src/mega_attention/runtime/workspace.py`,注释自述为 "the sglang-facing
runtime resource contract")。使用三步:

1. `FusedFaOprojArWorkspace.create(group_name, max_num_row_tiles, hidden, H_local, D,
   tp_size, rank, q_per_kv, paged=True, page_size=128, max_num_pages,
   max_num_pages_per_seq, ...)` —— 在一个容量 bucket 上一次性分配 workspace-owned 缓冲
   (O_scratch / C_sym / 控制位 / 小 varlen metadata),symm 分配做一次 rendezvous,把
   multicast / peer 指针烤进一个编译好的 kernel。
2. `compile(softmax_scale=..., ...)` —— 一次 `cute.compile`,之后任意 `num_row_tiles <=
   max_num_row_tiles` 的 batch 无需 host reset、无需重编译。
3. `build_prefill_meta(seqlens_q, seqlens_k=None, stream=None) -> RowDescMeta` —— **每 forward
   batch 一次**入口:从 seqlens 构造 row_desc + 做 seqlens 派生的 over-capacity 校验 + 上传 B 类
   metadata(cu_q/cu_k/fa_b/fa_mb/compact_tile_expected_count/actv)到 workspace capacity buffer。
   `launch_layer_prefill(meta, q, w_o, k_cache, v_cache, page_table, cache_seqlens, stream=None)`
   —— **每层**入口:A 类大张量(q/k_cache/v_cache/w_o/page_table/cache_seqlens)按指针借调零拷贝,
   只做 borrowed 容量校验 + 绑定 + launch,**不再重算/重传 B 类 metadata**(同一 forward 各层复用
   build_prefill_meta 那一份)。输出在 `ws.csym`,`C_sym[valid]` reshape 成 `[tot_q, hidden]` 即该层结果。

一个 workspace 因为 shape 是 dynamic,可服务模型所有 attention 层(同 H_local/D/hidden/
q_per_kv)。`tp_size==1` 时内部 AllReduce 退化为恒等,输出是本 rank 的 o_proj partial;
`tp_size>1` 时用 `multimem.ld_reduce/st` 在 C_sym multicast view 上做 in-place AllReduce,
输出是跨 rank 全量。

**约束**:仅 SM90;仅 prefill(含完整 prefill 与 `0 < q_len < k_len` 的 chunked/append);
causal **bottom-right 对齐**(offset = k_len − q_len);paged 必须 `page_size == 128`;标准 GQA
(`kv_head = q_head // q_per_kv`)。

### 2.2 Qwen3 MoE attention(SGLang 侧)

- `Qwen3MoeAttention` 在 `python/sglang/srt/models/qwen3_moe.py:436`。用标准
  `RadixAttention`(MHA/GQA,**非 MLA**),`head_dim=128`。q-norm/k-norm + RoPE 在
  `forward_core` 之前完成,backend 收到的 q/k/v 已 rope+norm。
- `o_proj = RowParallelLinear(..., reduce_results=False)`(`qwen3_moe.py:497`),权重布局
  `[hidden, K_local]`(`K_local = H_local*D`,row-major contiguous)。
- 接入点 `forward_core`(`qwen3_moe.py:680`):
  ```python
  attn_output = self.attn(q, k, v, fb, save_kv_cache=...)   # FA(写 KV + attention)
  output, _ = self.o_proj(attn_output)                       # O_proj,reduce_results=False
  return output                                              # partial,未 all-reduce
  ```
- 推迟的 TP AllReduce 由 `LayerCommunicator` 在 `prepare_mlp` 里做。纯 TP(无 dp-attention、
  无 EP)下,落在 `_gather_hidden_states_and_residual` 的
  `python/sglang/srt/layers/communicator.py:1113`:
  `hidden_states = attention_tensor_model_parallel_all_reduce(hidden_states)`,紧跟
  `hidden_states, residual = layernorm(hidden_states, residual)`(:1116)。

### 2.3 Qwen3-30B-A3B per-rank 形状

模型路径 `/models/Qwen3-30B-A3B`(本机已就位,16 个 safetensors 分片)。已核对其
`config.json`:`model_type=qwen3_moe, architectures=[Qwen3MoeForCausalLM],
hidden_size=2048, num_attention_heads=32, num_key_value_heads=4, head_dim=128,
num_hidden_layers=48, torch_dtype=bfloat16, num_experts=128, num_experts_per_tok=8,
moe_intermediate_size=768, max_position_embeddings=40960, rope_theta=1e6,
vocab_size=151936`。

- TP4(对 config 推导):`H_local = 32/4 = 8`,`H_kv_local = 4/4 = 1`(精确切分,非复制),
  `q_per_kv = 8`,`D = head_dim = 128`,`K_local = H_local*D = 1024`,`hidden = 2048`,
  `num_out = ceil(2048/128) = 16`(hidden 整除 128,C_sym 零拷贝 view 成立,见 §5.4)。
  `max_position_embeddings = 40960` → `max_num_pages_per_seq = ceil(40960/128) = 320`(§6 容量)。
- 这与 Qwen3-235B-A22B@TP8 的 per-rank `H8/kv1` 同构,kernel 路径一致;扩展到 235B 时
  主要是改启动参数与 workspace 容量。

## 3. 架构:模型层分叉

**核心**:在 `Qwen3MoeAttention.forward_core` 按 `forward_batch.forward_mode` 分叉。必须在
模型层做,因为标准 `AttentionBackend.forward` 只能返回 attention 的 `o`(pre-o_proj),无法
返回 megaAttention 的 post-o_proj `[tokens, hidden]`。

```text
forward_core(q, k, v):
  if forward_mode == ForwardMode.EXTEND 且 mega fast path gate 通过:
      # apply_qk_norm_rope 阶段已通过 fused-set-kv-buffer 写入新 token KV(见 §3.1)
      meta = mega_runtime.get_or_build_prefill_meta(forward_batch)      # 每 forward 建一次(内部 ws.build_prefill_meta)
      q3 = q.view(tot_q, H_local, D).contiguous()
      kc, vc = mega_runtime.paged_kv_view(layer.layer_id)               # pool buffer 的 paged view
      out = mega_ws.launch_layer_prefill(
                meta=meta.mega_meta, q=q3, w_o=self.o_proj.weight,       # 各层复用同一份 batch meta
                k_cache=kc, v_cache=vc,
                page_table=meta.page_table, cache_seqlens=meta.cache_seqlens)
      forward_batch.mega_attn_reduced = True                            # 通知 communicator 跳过 AR
      # C_sym 是 compact token-row 布局,零拷贝 view 出 [tot_q, hidden](见 §5.4)
      return mega_ws.csym.reshape(-1, num_out * 128)[:tot_q, :hidden]   # alias C_sym,跳过 self.o_proj
  else:  # decode
      attn_output = self.attn(q, k, v, fb)                              # fallback decode backend(fa3)
      return self.o_proj(attn_output)[0]
```

通过环境变量 `SGLANG_USE_MEGA_ATTENTION`(默认关)做总开关;关闭时所有路径字节级保持原状。
首版 fast path gate 必须同时满足:

- `forward_batch.forward_mode == ForwardMode.EXTEND`,用于 PD 分离下 prefill worker 的普通
  extend/prefill;不含 `MIXED` / `TARGET_VERIFY` / `DRAFT_EXTEND_V2` / `SPLIT_PREFILL` /
  `DLLM_EXTEND`。
- CUDA Hopper SM90;纯 TP;`attn_dp_size == 1`;不开 context parallel;不开 EP;不开量化通信。
- `page_size == 128`;普通 MHA/GQA KV pool,NHD flat buffer 可 view 成
  `[num_pages,128,H_kv,D]`;不走 SWA / vectorized_5d / FP8 KV 等特殊 pool。
- bf16 权重与激活;`self.o_proj.weight` 为 contiguous `[hidden,K_local]`,stride
  `(K_local,1)`。

gate 不满足时直接 fallback 到现有 `self.attn + self.o_proj + SGLang AllReduce` 路径。

### 3.1 KV 写入策略:强制 fused-set-kv-buffer

megaAttention 入口前必须保证本层新 token 的 K/V 已写入 SGLang KV pool。首版不走
`forward_core` 里的显式 `token_to_kv_pool.set_kv_buffer` fallback,而是在
`apply_qk_norm_rope()` 的 RoPE 阶段直接使用 SGLang 已有 fused-set-kv-buffer 机制:

- mega fast path 下,`apply_qk_norm_rope()` 必须产出已 q/k norm + RoPE 的 q/k,并把 k/v
  通过 `create_fused_set_kv_buffer_arg(value=v, layer=self.attn, forward_batch=forward_batch)`
  写入 `token_to_kv_pool`。
- 当前 Qwen3 MoE 的 `use_fused_qk_norm_rope` 分支只做 fused q/k norm + RoPE,不带
  fused-set-kv-buffer 参数。因此 mega fast path 首版应强制绕过该分支,走普通
  `apply_qk_norm + rotary_emb(..., fused_set_kv_buffer_arg=...)` 路径;这样不需要改 fused
  qk-norm-rope kernel,也能保证 KV 在 mega launch 前已写好。
- fast path gate 需要 assert `enable_fused_set_kv_buffer(forward_batch)` 且
  `self.compatible_with_fused_kv_buffer` 为 True;否则 fallback 到现有 `self.attn + self.o_proj`
  路径。

硬要求:进入 `mega_ws.launch_layer_prefill` 前,本层新 token KV 已在 pool 中;mega 路径不得依赖
`self.attn(..., save_kv_cache=True)` 写 KV,因为该路径会被跳过。

## 4. 数据映射表(mega 入参 ← SGLang 来源)

| mega 入参 | shape / dtype | SGLang 来源 |
|---|---|---|
| `q` | `[tot_q, H_local, D]` bf16 | `forward_core` 里 qk-norm+RoPE 后的 q,`.view(tot_q,H_local,D).contiguous()` |
| `w_o` | `[hidden, K_local]` bf16 | `self.o_proj.weight`(RowParallelLinear 原生布局,零拷贝;stride `(K_local,1)`) |
| `k_cache` / `v_cache` | `[num_pages, 128, H_kv, D]` | `token_to_kv_pool.get_key/value_buffer(layer_id)[:num_pages*128].view(num_pages,128,H_kv,D)` |
| `page_table` | `[B, max_pages_per_seq]` int32 | `req_to_token[req_pool_indices, ::128] // 128`(复用 FA3 算法,见 `flashattention_backend.py:718`) |
| `cache_seqlens` | `[B]` int32 | `forward_batch.seq_lens`(prefix+extend 总 KV 长度) |
| `seqlens_q` | per-seq 新 query 长度 | `forward_batch.extend_seq_lens` |
| `softmax_scale` | 标量 | `layer.scaling = head_dim**-0.5`(与 mega 默认 `D**-0.5` 一致;compile 时显式传) |

**KV layout 依据**:`MHATokenToKVPool` 的每层 K/V buffer 是 flat token-indexed
`[size+page_size, H_kv, D]`(`memory_pool.py:1256`);`--page-size 128` 时 paged allocator
保证发放的 slot 是 page 对齐的 128 连续整段(物理 page p ↔ flat slot `[p*128, p*128+128)`),
所以 `view(num_pages,128,H_kv,D)` 恰好是 mega 要的 paged 布局,`req_to_token//128` 得到的
物理 page index 正好索引到它。新 token 的 KV 写入策略见 §3.1;megaAttention launch 前必须
保证本层新 token 已在 pool 中,prefix KV 上一轮已在 pool,page_table 覆盖全部。该语义与
FA3 prefill 同源 →
前缀缓存 / chunked prefill 自动正确,bottom-right causal 对应 `q_len<k_len`。

## 5. 全融合 AllReduce 机制

### 5.1 workspace 创建与 symm-mem

- `group_name = get_tp_group().device_group.group_name`(`distributed/parallel_state.py`,
  纯 TP 下 `_ATTN_TP == _TP`)。
- 创建前 `symm_mem.enable_symm_mem_for_group(group_name)`;mega `create()` 内部用
  `symm_mem.empty + rendezvous` 在该 group 上分配独立 buffer(SGLang 的
  `TorchSymmMemCommunicator` 已证实该 group_name 可做 NVLS multicast)。
- `tp_size = TP`(如 4),`rank = tp_rank`。
- **创建时机**:首个 extend forward 懒初始化。SGLang SPMD 下所有 rank 锁步进入同一 prefill,
  `symm_mem.empty + rendezvous` 是 collective,锁步即安全;`cute.compile` 是 per-rank 本地操作。
  容量参数从 `server_args` 推导,与首个 batch 大小无关。**退路**:若遇到跨 rank 顺序问题,改为
  在 ModelRunner KV pool 初始化后加一个显式同步 init hook。

### 5.2 跳过 SGLang 的 AllReduce

mega prefill 输出已是全量 all-reduced,必须跳过 `communicator.py:1113` 的二次 AllReduce:

- prefill+mega 路径给 `forward_batch` 打 flag `mega_attn_reduced = True`。
- 在 `_gather_hidden_states_and_residual` 的纯 TP 分支里,若
  `getattr(forward_batch, "mega_attn_reduced", False)` 为 True,必须直接执行
  `hidden_states, residual = layernorm(hidden_states, residual)` 并返回该分支结果。
  这要绕过普通 all-reduce、量化 all-reduce,以及 `forward_with_allreduce_fusion` 一类 fused
  allreduce+layernorm 路径;**只跳通信,保留后续 RMSNorm/add residual 语义**。
- 该改动被 mega 总开关 gate;关闭时 flag 永远为 False,默认行为不变。

### 5.3 symm-mem 共存

mega 与 SGLang 各自在 TP group 上 `empty + rendezvous` 独立 buffer。由于已跳过 SGLang 的
attention AllReduce,两边 attention 通信不冲突;SGLang 自身 symm-mem(若开)仅用于 MoE 等其他
路径。首版用 `--disable-cuda-graph` + 懒初始化降低 rendezvous 顺序与 capture stream 风险。

### 5.4 C_sym 输出:零拷贝喂给 RMS norm + 生命周期契约

mega 的最终 AllReduce 结果**就地落在 C_sym(symmetric memory)**,无需拷贝出来即可作为后续
`post_attention_layernorm`(RMS norm)的输入。依据:

- **布局 gap-free**:`row_desc.py` 明确把 C_sym 设计成 compact token-row 布局——"compact tiles
  densely tile the global compact token axis `[0, tot_q)` with NO per-sequence padding gap, so
  the downstream AllReduce consumer reads C_sym gap-free per token"。物理形状
  `[max_num_row_tiles, 128, num_out, 128]`,token 维第 r 行 = 第 r 个 packed query token(与
  输入 Q 的 `[tot_q]` 同序),tail tile 的 padding 行不占 C_sym 行。
- **隐藏维无 padding**:`num_out = ceil(hidden/128)`;Qwen3 hidden 均为 128 整数倍
  (30B=2048→16,235B=4096→32),故 `num_out*128 == hidden`。于是
  `ws.csym.reshape(-1, num_out*128)[:tot_q, :hidden]` 是**纯 view(alias C_sym),零拷贝**,
  直接当 `hidden_states` 进 communicator 的 `layernorm(hidden_states, residual)`。
- **生命周期约束**:C_sym 是**跨 48 层复用的同一块 symm buffer**。RMS norm 必须在**下一层
  `launch_layer_prefill` 覆盖 C_sym 之前**消费完——SGLang 逐层执行天然满足(`prepare_mlp` 的
  add_rmsnorm 在本层、下一层 attention 之前完成),residual 流是另一块独立 tensor,不受 C_sym
  覆盖影响。**实现约束:不要把该 C_sym view 缓存跨层复用。**
- **in-place 注脚**:即便 add_rmsnorm in-place 写回 hidden_states(=C_sym)也无害(下层覆盖);
  零拷贝只针对 RMS norm 的输入侧,这一侧成立。仅当某模型 hidden 非 128 倍数时末尾 out-tile 有
  padding,`[:, :hidden]` 切片可能非连续触发一次 copy——Qwen3 不涉及。

## 6. workspace 生命周期与容量

- `max_num_row_tiles` 须覆盖真实 `sum_i ceil(q_i / 128)`,其中 `q_i` 是每条请求本次
  extend/prefill 的 query token 数。创建 workspace 时用 SGLang 已解析后的配置估算上界:
  - `T = server_args.max_prefill_buffer_tokens()`;PD prefill 首版不开 PP dynamic chunking 时,
    等价于最终 `chunked_prefill_size`。若该值为 0 或未启用 chunking,mega fast path 不启用。
  - `N = server_args.prefill_max_requests or model_runner.max_running_requests`。`prefill_max_requests`
    更贴近单个 prefill batch 的请求数上界;未配置时用 runtime 已计算出的
    `max_running_requests`。
  - 严格上界取 `N + floor((T - N) / 128)`(`T >= N`);等价保守写法是
    `ceil(T / 128) + N - 1`。若 `T < N`,实际最多只有 `T` 个非空请求,上界取 `T`。
  旧的 `max(ceil(chunked_prefill_size/128), max_num_batch)` 只能覆盖"全长单请求"或"全短请求"
  两个端点,不能覆盖长短混合 batch。首版实现仍需在进入 `launch_layer_prefill` 前用实际
  `extend_seq_lens` 计算 `num_row_tiles` 并 assert/fallback,防止污染 workspace。
- `max_num_batch = max_running_requests`;`max_num_pages = token_to_kv_pool.size // 128`;
  `max_num_pages_per_seq = ceil(max_context_len / 128)`。
- paged metadata(page_table / cache_seqlens / seqlens_q)每个 forward 建一次,48 层复用
  (缓存在 `forward_batch` 上,按 forward_batch 身份判失效)。
- mega 侧 row_desc / B 类 metadata 也由 `build_prefill_meta` 每 forward 建一次并上传一次,48 层
  复用同一份(megaAttention 已落实二段式 API:`build_prefill_meta` + `launch_layer_prefill(meta=...)`,
  B 类 metadata 不被 exit cleaner 清零,跨层存活);`launch_layer_prefill` 每层只做 borrowed 绑定 + launch。

## 7. 改动清单(文件)

- 新增 `python/sglang/srt/layers/attention/mega_attention_runtime.py`:
  - per-rank `FusedFaOprojArWorkspace` 的懒初始化与持有(从 `server_args` + `token_to_kv_pool`
    推容量);
  - `get_or_build_prefill_meta(forward_batch)`:建 page_table / cache_seqlens / seqlens_q,并每
    forward 调用一次 `ws.build_prefill_meta(seqlens_q)`(返回的 mega RowDescMeta 缓存在
    forward_batch 上,48 层复用);
  - `paged_kv_view(layer_id)`:返回 pool buffer 的 `[num_pages,128,H_kv,D]` view;
  - 总开关 `SGLANG_USE_MEGA_ATTENTION` 读取(走 `environ.py` 规范)。
- 改 `python/sglang/srt/models/qwen3_moe.py`:`Qwen3MoeAttention.__init__`(取 runtime/workspace
  handle)与 `forward_core`(prefill/decode 分叉、写 KV、调 mega、打 flag、跳 o_proj)。
- 改 `python/sglang/srt/layers/communicator.py`:`_gather_hidden_states_and_residual` 里
  AllReduce 段加 `mega_attn_reduced` flag 守卫。
- 可能改 `python/sglang/srt/model_executor/forward_batch_info.py`:`ForwardBatch` 增加
  `mega_attn_reduced` 字段(或 `setattr` 动态属性,首版可不入 dataclass)。
- 环境变量经 `python/sglang/srt/environ.py` 注册(遵守 env-var-conventions)。

## 8. 启动配置(首个 e2e)

```bash
SGLANG_USE_MEGA_ATTENTION=1 python -m sglang.launch_server \
  --model-path /models/Qwen3-30B-A3B \
  --tp 4 --page-size 128 \
  --attention-backend fa3 \
  --chunked-prefill-size 8192 \
  --disable-cuda-graph \
  --dtype bfloat16
```

约束:纯 TP(不开 dp-attention、不开 EP、不量化);decode 用 fa3(支持 page_size=128 paged
decode);prefill 走 mega 且保持 eager;`sglang` 需先 `pip install -e`(当前环境未安装)。
decode 稳定后再考虑开 cuda graph(prefill 仍 eager)。

## 9. 风险

1. **KV 写入策略**:mega 路径依赖 RoPE 阶段 fused-set-kv-buffer 写 KV。由于当前
   fused qk-norm-rope 分支不带 KV 写入,mega fast path 首版需强制走普通 qk-norm + rotary
   fused-set-kv-buffer 路径,并在 gate 中 assert fused-set-kv-buffer 可用。
2. **symm-mem 共存 / rendezvous 顺序**:首版 `--disable-cuda-graph` + 懒初始化锁步降低风险。
3. **page view 对齐**:依赖 paged allocator 的 128-slot 连续保证;集成时需实测核对
   `req_to_token//128` 与物理 slot 的对应(尤其 pool 是否保留首槽)。
4. **CUDA graph 捕获 prefill**:必须保证 prefill eager,否则与 mega 的 eager launch 冲突。
5. **q contiguous / dtype**:`q.view(...).contiguous()`;确认 bf16、未量化。
6. **w_o stride**:确认 `self.o_proj.weight` 为 `[hidden,K_local]` 且 stride `(K_local,1)`、bf16。
7. **workspace 容量**:`max_num_row_tiles` 需按 `T` 和 `N` 的上界估算,并用实际
   `sum_i ceil(q_i/128)` 做运行时 assert/fallback,不能只用
   `max(ceil(total_q/128), batch)`。

## 10. 验证计划

1. **基线**:`SGLANG_USE_MEGA_ATTENTION=0`,fa3 prefill+decode,记录固定 prompt 的 greedy 输出。
2. **开 mega**:同 prompt greedy,比对输出 token 序列一致 / logprob 接近。
3. **单层数值 gate**:一次 prefill 抓某层 mega 输出 vs `fa3 + o_proj + 手动 all_reduce`,
   `err_rel` 应在 bf16 级(~1e-3,对齐 megaAttention README 的 7e-4~2e-3)。
4. **端到端**:`Engine.generate` 或 server `/generate` 跑一条多请求/多轮,确认输出连贯、无 hang。
5. 通过后再扩到 Qwen3-235B-A22B@TP8(改 `--model-path`、`--tp 8`,workspace 容量随 `hidden=4096`
   / `H_local=8` 调整)。

## 11. 后续(本设计之外)

- decode 路径若 megaAttention 将来支持,可纳入;当前固定 fallback。
- 开 cuda graph 捕获 prefill 的 mega 路径(需验证 capture stream 契约)。
- row_desc 跨层缓存优化已在 megaAttention 侧落实(`build_prefill_meta`/`launch_layer_prefill` 二段式);
  AR comm/compute overlap 调参。
- 把模型层分叉抽象成更干净的 hook(减少对 `qwen3_moe.py` / `communicator.py` 的直接侵入)。
